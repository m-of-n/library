; GENERATED by bin/derive from distilled/fields.yaml — do not edit
; source: draft-ietf-vcon-vcon-core-04 -04

vcon = {
  ? "vcon" => tstr,  ; §4.1.1 MUST
  "uuid" => tstr,  ; §4.1.2 MUST
  ? "extensions" => [* tstr],  ; §4.1.3 SHOULD
  ? "critical" => [* tstr],  ; §4.1.4 MUST
  "created_at" => tstr,  ; §4.1.5 MUST
  ? "updated_at" => tstr,  ; §4.1.6 MUST
  ? "subject" => tstr,  ; §4.1.7 none
  ? "redacted" => { * tstr => any },  ; §4.1.8 / §4.1.8.1 MUST
  ? "amended" => { * tstr => any },  ; §4.1.9 / §4.1.9.1 MUST
  ? "parties" => [* { * tstr => any }],  ; §4.1.10 none
  ? "dialog" => [* { * tstr => any }],  ; §4.1.11 none
  ? "analysis" => [* { * tstr => any }],  ; §4.1.12 none
  ? "attachments" => [* { * tstr => any }],  ; §4.1.13 none
}

party = {
  ? "tel" => tstr,  ; §4.2.1 SHOULD
  ? "sip" => tstr,  ; §4.2.2 none
  ? "stir" => tstr,  ; §4.2.3 none
  ? "mailto" => tstr,  ; §4.2.4 MAY
  ? "name" => tstr,  ; §4.2.5 SHOULD
  "did" => tstr,  ; §4.2.6 MUST
  ? "validation" => tstr,  ; §4.2.7 SHOULD NOT
  ? "gmlpos" => tstr,  ; §4.2.8 none
  "civicaddress" => any,  ; §4.2.9 MUST
  ? "uuid" => tstr,  ; §4.2.10 MAY
  ? "type" => tstr,  ; §4.2.11 SHOULD
  ? "org" => tstr,  ; §4.2.12 none
  ? "dept" => tstr,  ; §4.2.13 none
}

dialog = {
  "type" => tstr,  ; §4.3.1 MUST
  ? "start" => tstr,  ; §4.3.2 SHOULD
  "duration" => any,  ; §4.3.3 MUST
  ? "parties" => any,  ; §4.3.4 MUST NOT
  ? "originator" => any,  ; §4.3.5 MUST NOT
  ? "recordings" => any,  ; §4.3.6 MUST NOT
  ? "recording_set" => any,  ; §4.3.7 MUST NOT
  ? "mediatype" => any,  ; §4.3.8 MUST NOT
  ? "filename" => tstr,  ; §4.3.9 MUST NOT
  "disposition" => tstr,  ; §4.3.11 MUST
  ? "session_id" => any,  ; §4.3.12 MUST NOT
  ? "application" => any,  ; §4.3.15 none
  ? "message_id" => tstr,  ; §4.3.16 MUST NOT
  ? "party_history Objects Array" => any,  ; §4.3.13 MUST NOT
}

attachment = {
  ? "purpose" => tstr,  ; §4.4.1 none
  ? "start" => tstr,  ; §4.4.2 none
  "party" => any,  ; §4.4.3 MUST
  ? "dialog" => any,  ; §4.4.4 none
  "mediatype" => any,  ; §4.4.5 MUST
  ? "filename" => tstr,  ; §4.4.6 none
}

analysis = {
  ? "type" => tstr,  ; §4.5.1 SHOULD
  ? "dialog" => any,  ; §4.5.2 none
  ? "attachment" => any,  ; §4.5.3 none
  ? "mediatype" => any,  ; §4.5.4 SHOULD
  ? "filename" => tstr,  ; §4.5.5 none
  ? "vendor" => tstr,  ; §4.5.6 none
  ? "product" => tstr,  ; §4.5.7 none
  ? "schema" => tstr,  ; §4.5.8 none
}
